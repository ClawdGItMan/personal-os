# Copy to `.env` and fill in. These are the Personal OS Supabase project's
# PUBLIC values (safe to ship in a client) — the same backend the web app uses.
# Get them from: Supabase dashboard → Project Settings → API, or `vercel env pull`.
EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key
# Dev-only simulator auto-login (see src/auth/devLogin.ts + scripts/dev/seed-sim-user.sql)
EXPO_PUBLIC_DEV_LOGIN_EMAIL=
EXPO_PUBLIC_DEV_LOGIN_PASSWORD=
# Assistant/API base. Leave blank to use the built-in prod default
# (https://personal-os-azure-eight.vercel.app, baked into
# src/lib/assistant/api.ts); set to http://localhost:3000 to hit `next dev`.
EXPO_PUBLIC_API_URL=
